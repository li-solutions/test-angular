export enum UserRoles {
  USER = 'user',
  ADMIN = 'admin',
}

export enum Routes {
  HOME = '',
  SIGN_IN = 'sign-in',
  SIGN_UP = 'sign-up',
  POSTS = 'posts',
  UNAVAILABLE = '403',
  NOT_FOUND = '404',
}
